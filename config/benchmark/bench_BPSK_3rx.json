// Comments are allowed in this JSON config file
// The package rjsmin is used to strip the comments when parsing

{
    "configBase": "bench_3_chan_rx_base.json", // base config file name
    "Main":{
	// For the main code
	"workerId": "bench_BPSK",
	"protocols": {
	    "UHF" : "bench_BPSK"
	}
    },
    "GPU":{
	"UHF" :{
	    "xcorrMaskSize": 5 // BPSK is hardcoded at 5 at the momemt
	}
    }
}
